class Point3D { 
    x: number;
    y: number;
    z: number;
}

function abc() {
    let point: Point = new Point();

    function Move() {
        point.x = 1; 
        point.y = 2;
    } 
}

let point3D: Point3D = new Point3D();

function Move1() {
    point3D.x = 1;
    point3D.y = 2;
    point3D.z = 3;
}